import styled from 'styled-components';

const Bottom = styled.section`
    text-align: center;
`;
export default function Bottom({
    value,
    width,
    fontSize,
    background,
    onClick,
}) {
    return (
        <Bottom>
            
        </Bottom>
    );
}